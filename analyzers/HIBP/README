Bitly is a URL shortening service. They have an API endpoint that will
allow us to expand their shortened URLS

Add the following to application.conf to enable analyzer

```
Bity {
        key="Get your key from https://dev.bitly.com"
       }
```

To test the analyzer from cmdline

python bitly_analyzer.py < input

Testing
--------
cd /opt/thehive

bin/thehive -Dconfig.file=conf/application.conf

bin/cortex -Dconfig.file=/opt/cortex/conf/application.conf
